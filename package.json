{
  "name": "fund-trend-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --no-warnings server.mjs",
    "init-db": "node --no-warnings scripts/init-db.mjs",
    "sync:funds": "node --no-warnings scripts/sync-fund-data.mjs"
  }
}
